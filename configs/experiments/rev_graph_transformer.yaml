# configs/experiments/rev_graph_transformer.yaml
project:
  experiment_name: "rev_graph_transformer"
encoder:
  type: "graph_transformer"
  hidden_dim: 64
  n_layers: 2
  n_heads: 4
  dropout: 0.1
training:
  algorithm: "gail+reinforce"
  gail_epochs: 50
  reinforce_epochs: 100
